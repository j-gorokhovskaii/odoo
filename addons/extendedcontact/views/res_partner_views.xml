<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <!-- Extend the simplified partner form view -->
        <record id="view_partner_simple_form_inherit_dob" model="ir.ui.view">
            <field name="name">res.partner.simple.form.inherit.dob</field>
            <field name="model">res.partner</field>
            <field name="inherit_id" ref="base.view_partner_simple_form"/>
            <field name="arch" type="xml">
                <!-- Add birthdate field after the function field -->
                <xpath expr="//field[@name='function']" position="after">
                    <field name="birthdate" 
                           invisible="is_company" 
                           widget="date"
                           options="{'format': 'dd/mm/yyyy'}"
                           placeholder="e.g. 15/03/1990"/>
                    <field name="age" 
                           invisible="is_company or not birthdate" 
                           readonly="1"/>
                </xpath>
            </field>
        </record>

        <!-- Extend the partner tree view to optionally show age -->
        <record id="view_partner_tree_inherit_dob" model="ir.ui.view">
            <field name="name">res.partner.tree.inherit.dob</field>
            <field name="model">res.partner</field>
            <field name="inherit_id" ref="base.view_partner_tree"/>
            <field name="arch" type="xml">
                <!-- Add age field as optional column -->
                <xpath expr="//field[@name='email']" position="after">
                    <field name="age" 
                           optional="hide" 
                           invisible="is_company"/>
                </xpath>
            </field>
        </record>

        <!-- Extend the main partner form view to move fields under Tags section -->
        <record id="view_partner_form_inherit_extended_tags" model="ir.ui.view">
            <field name="name">res.partner.form.inherit.extended.tags</field>
            <field name="model">res.partner</field>
            <field name="inherit_id" ref="base.view_partner_form"/>
            <field name="arch" type="xml">
                <!-- Move specified fields under the Tags section -->
                <xpath expr="//field[@name='category_id']" position="after">
                    <field name="birthdate" 
                           invisible="is_company"
                           widget="date"
                           options="{'format': 'dd/mm/yyyy'}"
                           placeholder="e.g. 15/03/1990"
                           string="Date of Birth"/>
                    <field name="age" 
                           invisible="is_company or not birthdate" 
                           readonly="1"
                           string="Age (Years)"/>
                    <field name="age_detailed" 
                           invisible="is_company or not birthdate"
                           readonly="1"
                           string="Detailed Age"/>
                    <field name="birthday_countdown" 
                           invisible="is_company or not birthdate"
                           readonly="1"
                           string="Time to Next Birthday"/>
                    <field name="gender" 
                           invisible="is_company"/>
                    <field name="marital_status" 
                           invisible="is_company"/>
                </xpath>
            </field>
        </record>

        <!-- Extend the main partner form view to add extended information tab -->
        <record id="view_partner_form_inherit_extended_tab" model="ir.ui.view">
            <field name="name">res.partner.form.inherit.extended.tab</field>
            <field name="model">res.partner</field>
            <field name="inherit_id" ref="base.view_partner_form"/>
            <field name="arch" type="xml">
                <!-- Add new tab after Internal Notes tab -->
                <xpath expr="//page[@name='internal_notes']" position="after">
                    <page string="Extended Information" name="extended_information" invisible="is_company">
                        <!-- Physical Characteristics Section -->
                        <group string="Physical Characteristics" col="2">
                            <field name="height" 
                                   widget="float"
                                   placeholder="e.g. 175.5"/>
                            <field name="weight" 
                                   widget="float"
                                   placeholder="e.g. 70.5"/>
                            <field name="build"/>
                            <field name="complexion"/>
                            <field name="eye_color"/>
                            <field name="hair_color"/>
                            <field name="hair_style"/>
                            <field name="blood_type"/>
                        </group>

                        <!-- Distinguishing Features Section -->
                        <group string="Distinguishing Features" col="1">
                            <field name="distinguishing_features" 
                                   placeholder="e.g. Scar on left cheek, tattoo on right arm"
                                   nolabel="1"/>
                        </group>

                        <!-- Demographics Section -->
                        <group string="Demographics" col="2">
                            <field name="ethnicity"/>
                            <field name="race"/>
                            <field name="primary_language"/>
                            <field name="country_of_birth"/>
                            <field name="citizenship_status"/>
                        </group>

                        <!-- Identification Section -->
                        <group string="Identification" col="2">
                            <field name="ssn" 
                                   placeholder="XXX-XX-XXXX"/>
                            <field name="driver_license_number"/>
                            <field name="driver_license_state"/>
                            <field name="passport_number"/>
                            <field name="passport_country"/>
                        </group>

                        <!-- Employment &amp; Education Section -->
                        <group string="Employment &amp; Education" col="2">
                            <field name="occupation" 
                                   placeholder="e.g. Sales Director"/>
                            <field name="employer"/>
                            <field name="education_level"/>
                        </group>

                        <!-- Military Service Section -->
                        <group string="Military Service" col="2">
                            <field name="military_service"/>
                            <field name="military_branch" 
                                   invisible="not military_service"/>
                        </group>

                        <!-- Medical Information Section -->
                        <group string="Medical Information" col="1">
                            <field name="medical_conditions" 
                                   placeholder="e.g. Diabetes, allergies to penicillin"
                                   nolabel="1"/>
                        </group>

                        <!-- Emergency Contact Section -->
                        <group string="Emergency Contact" col="2">
                            <field name="emergency_contact_name"/>
                            <field name="emergency_contact_relationship"/>
                        </group>
                    </page>
                </xpath>
            </field>
        </record>

        <!-- Extend the partner search view to add filters for extended fields -->
        <record id="view_res_partner_filter_extended" model="ir.ui.view">
            <field name="name">res.partner.select.extended</field>
            <field name="model">res.partner</field>
            <field name="inherit_id" ref="base.view_res_partner_filter"/>
            <field name="arch" type="xml">
                <!-- Add birthdate-based filters -->
                <xpath expr="//filter[@name='inactive']" position="after">
                    <separator/>
                    <filter string="Has Birth Date" 
                            name="has_birthdate" 
                            domain="[('birthdate', '!=', False)]"/>
                    <filter string="No Birth Date" 
                            name="no_birthdate" 
                            domain="[('birthdate', '=', False)]"/>
                    <filter string="Born in 2000s" 
                            name="born_2000s" 
                            domain="[('birthdate', '>=', '2000-01-01'), ('birthdate', '&lt;', '2010-01-01')]"/>
                    <filter string="Born in 1990s" 
                            name="born_1990s" 
                            domain="[('birthdate', '>=', '1990-01-01'), ('birthdate', '&lt;', '2000-01-01')]"/>
                    <filter string="Born in 1980s" 
                            name="born_1980s" 
                            domain="[('birthdate', '>=', '1980-01-01'), ('birthdate', '&lt;', '1990-01-01')]"/>
                </xpath>

                <!-- Add height-based filters -->
                <xpath expr="//filter[@name='born_1980s']" position="after">
                    <separator/>
                    <filter string="Has Height" 
                            name="has_height" 
                            domain="[('height', '!=', False)]"/>
                    <filter string="No Height" 
                            name="no_height" 
                            domain="[('height', '=', False)]"/>
                    <filter string="Height 150cm+" 
                            name="height_150_plus" 
                            domain="[('height', '>=', 150)]"/>
                    <filter string="Height 180cm+" 
                            name="height_180_plus" 
                            domain="[('height', '>=', 180)]"/>
                </xpath>

                <!-- Add weight-based filters -->
                <xpath expr="//filter[@name='height_180_plus']" position="after">
                    <separator/>
                    <filter string="Has Weight" 
                            name="has_weight" 
                            domain="[('weight', '!=', False)]"/>
                    <filter string="No Weight" 
                            name="no_weight" 
                            domain="[('weight', '=', False)]"/>
                    <filter string="Weight 50kg+" 
                            name="weight_50_plus" 
                            domain="[('weight', '>=', 50)]"/>
                    <filter string="Weight 80kg+" 
                            name="weight_80_plus" 
                            domain="[('weight', '>=', 80)]"/>
                </xpath>

                <!-- Add gender filters -->
                <xpath expr="//filter[@name='weight_80_plus']" position="after">
                    <separator/>
                    <filter string="Male" 
                            name="male" 
                            domain="[('gender', '=', 'male')]"/>
                    <filter string="Female" 
                            name="female" 
                            domain="[('gender', '=', 'female')]"/>
                </xpath>

                <!-- Add marital status filters -->
                <xpath expr="//filter[@name='female']" position="after">
                    <separator/>
                    <filter string="Single" 
                            name="single" 
                            domain="[('marital_status', '=', 'single')]"/>
                    <filter string="Married" 
                            name="married" 
                            domain="[('marital_status', '=', 'married')]"/>
                    <filter string="Divorced" 
                            name="divorced" 
                            domain="[('marital_status', '=', 'divorced')]"/>
                </xpath>

                <!-- Add eye color filters -->
                <xpath expr="//filter[@name='divorced']" position="after">
                    <separator/>
                    <filter string="Brown Eyes" 
                            name="brown_eyes" 
                            domain="[('eye_color', '=', 'brown')]"/>
                    <filter string="Blue Eyes" 
                            name="blue_eyes" 
                            domain="[('eye_color', '=', 'blue')]"/>
                    <filter string="Green Eyes" 
                            name="green_eyes" 
                            domain="[('eye_color', '=', 'green')]"/>
                </xpath>

                <!-- Add hair color filters -->
                <xpath expr="//filter[@name='green_eyes']" position="after">
                    <separator/>
                    <filter string="Brown Hair" 
                            name="brown_hair" 
                            domain="[('hair_color', '=', 'brown')]"/>
                    <filter string="Black Hair" 
                            name="black_hair" 
                            domain="[('hair_color', '=', 'black')]"/>
                    <filter string="Blonde Hair" 
                            name="blonde_hair" 
                            domain="[('hair_color', '=', 'blonde')]"/>
                    <filter string="Red Hair" 
                            name="red_hair" 
                            domain="[('hair_color', '=', 'red')]"/>
                </xpath>

                <!-- Add race filters -->
                <xpath expr="//filter[@name='red_hair']" position="after">
                    <separator/>
                    <filter string="White" 
                            name="white_race" 
                            domain="[('race', '=', 'white')]"/>
                    <filter string="Black" 
                            name="black_race" 
                            domain="[('race', '=', 'black')]"/>
                    <filter string="Asian" 
                            name="asian_race" 
                            domain="[('race', '=', 'asian')]"/>
                    <filter string="Hispanic" 
                            name="hispanic_ethnicity" 
                            domain="[('ethnicity', '=', 'hispanic')]"/>
                </xpath>

                <!-- Add citizenship filters -->
                <xpath expr="//filter[@name='hispanic_ethnicity']" position="after">
                    <separator/>
                    <filter string="U.S. Citizen" 
                            name="us_citizen" 
                            domain="[('citizenship_status', '=', 'citizen')]"/>
                    <filter string="Permanent Resident" 
                            name="permanent_resident" 
                            domain="[('citizenship_status', '=', 'permanent_resident')]"/>
                    <filter string="Military Service" 
                            name="military_service" 
                            domain="[('military_service', '=', True)]"/>
                </xpath>
            </field>
        </record>
    </data>
</odoo> 